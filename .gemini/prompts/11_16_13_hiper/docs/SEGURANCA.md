# Segurança/consistência
Sessão + RBAC + producerId + eventId + ownership.
OAuth state/nonce/PKCE quando aplicável. Segredos server-side.
Idempotency-Key em create/publish/pause/resume/stop/budget/send.
Rate limit. Webhook com validação de assinatura/replay quando suportado.
Audit before/after sem segredo. correlationId. Confirmação em ação crítica.
