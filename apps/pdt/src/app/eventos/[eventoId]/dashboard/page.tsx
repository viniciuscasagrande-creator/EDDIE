'use client'; import { useEffect,useState } from 'react'; import { useParams } from 'next/navigation'; import { Kpi,EmptyChart } from '../../../../components/eventos/EventOsShell';
export default function Page(){const {eventoId}=useParams<{eventoId:string}>(); const [d,setD]=useState<any>(null);useEffect(()=>{fetch(`/api/eventos/${eventoId}/os-resumo`).then(r=>r.ok?r.json():null).then(setD).catch(()=>setD(null))},[eventoId]);return <div className="space-y-4"><div><h2 className="text-xl font-bold text-white">Dashboard do Evento</h2><p className="text-sm text-slate-400">Vendas, ingressos, pedidos, capacidade, caixa e performance deste evento.</p></div><div className="grid md:grid-cols-2 xl:grid-cols-4 gap-3"><Kpi label="GMV do Evento" value={d?.gmvFormatado||'—'} help="Valor bruto vendido"/><Kpi label="Ingressos vendidos" value={String(d?.ingressosVendidos??'—')} help="Quantidade confirmada"/><Kpi label="Pedidos pagos" value={String(d?.pedidosPagos??'—')} help="Pedidos confirmados"/><Kpi label="Saldo do evento" value={d?.saldoFormatado||'—'} help="Ledger vinculado ao evento"/></div><div className="grid xl:grid-cols-2 gap-4"><EmptyChart title="Evolução de vendas"/><EmptyChart title="Receita, taxa Disk e repasse do produtor"/></div></div>}
